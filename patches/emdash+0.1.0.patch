diff --git a/node_modules/emdash/dist/astro/middleware.mjs b/node_modules/emdash/dist/astro/middleware.mjs
index af52a74..a14d596 100644
--- a/node_modules/emdash/dist/astro/middleware.mjs
+++ b/node_modules/emdash/dist/astro/middleware.mjs
@@ -1440,6 +1440,12 @@ let i18nInitialized = false;
 * lifetime.
 */
 let setupVerified = false;
+let setupProbeWarningLogged = false;
+function isMissingTableError(error) {
+	const message = error instanceof Error ? error.message : String(error);
+	const lowerMessage = message.toLowerCase();
+	return lowerMessage.includes("no such table") || lowerMessage.includes("table") && lowerMessage.includes("does not exist") || lowerMessage.includes("relation") && lowerMessage.includes("does not exist");
+}
 /**
 * Get EmDash configuration from virtual module
 */
@@ -1521,8 +1527,12 @@ const onRequest = defineMiddleware(async (context, next) => {
 				const { getDb } = await import("../loader-fz8Q_3EO.mjs").then((n) => n.r);
 				await (await getDb()).selectFrom("_emdash_migrations").selectAll().limit(1).execute();
 				setupVerified = true;
-			} catch {
-				return context.redirect("/_emdash/admin/setup");
+			} catch (error) {
+				if (isMissingTableError(error)) return context.redirect("/_emdash/admin/setup");
+				if (!setupProbeWarningLogged) {
+					setupProbeWarningLogged = true;
+					console.warn("[emdash] Public-route setup probe failed; skipping setup redirect because the database may still be configured:", error);
+				}
 			}
 			const response = await next();
 			setBaselineSecurityHeaders(response);
diff --git a/node_modules/emdash/src/astro/middleware.ts b/node_modules/emdash/src/astro/middleware.ts
index 6747522..fb377d5 100644
--- a/node_modules/emdash/src/astro/middleware.ts
+++ b/node_modules/emdash/src/astro/middleware.ts
@@ -63,6 +63,18 @@ let i18nInitialized = false;
  * lifetime.
  */
 let setupVerified = false;
+/** Whether we've already logged a non-setup public probe failure in this worker */
+let setupProbeWarningLogged = false;
+
+function isMissingTableError(error: unknown): boolean {
+	const message = error instanceof Error ? error.message : String(error);
+	const lowerMessage = message.toLowerCase();
+	return (
+		lowerMessage.includes("no such table") ||
+		(lowerMessage.includes("table") && lowerMessage.includes("does not exist")) ||
+		(lowerMessage.includes("relation") && lowerMessage.includes("does not exist"))
+	);
+}
 
 /**
  * Get EmDash configuration from virtual module
@@ -216,9 +228,19 @@ export const onRequest = defineMiddleware(async (context, next) => {
 						.limit(1)
 						.execute();
 					setupVerified = true;
-				} catch {
-					// Table doesn't exist -> fresh database, redirect to setup
-					return context.redirect("/_emdash/admin/setup");
+				} catch (error) {
+					if (isMissingTableError(error)) {
+						// Table doesn't exist -> fresh database, redirect to setup
+						return context.redirect("/_emdash/admin/setup");
+					}
+
+					if (!setupProbeWarningLogged) {
+						setupProbeWarningLogged = true;
+						console.warn(
+							"[emdash] Public-route setup probe failed; skipping setup redirect because the database may still be configured:",
+							error,
+						);
+					}
 				}
 			}
 
diff --git a/node_modules/emdash/src/astro/routes/admin.astro b/node_modules/emdash/src/astro/routes/admin.astro
index 9b5709b..2f45352 100644
--- a/node_modules/emdash/src/astro/routes/admin.astro
+++ b/node_modules/emdash/src/astro/routes/admin.astro
@@ -6,9 +6,6 @@
  * AdminWrapper imports plugin admin modules and passes them to AdminApp.
  */
 import "@emdash-cms/admin/styles.css";
-// Use package-qualified import so Astro generates a proper module URL
-// (relative imports resolve to absolute paths which break client hydration)
-import AdminWrapper from "emdash/routes/PluginRegistry";
 
 export const prerender = false;
 ---
@@ -75,7 +72,10 @@ export const prerender = false;
 					<p>Loading EmDash...</p>
 				</div>
 			</div>
-			<AdminWrapper client:only="react" />
+			<script>
+				import mountEmdashAdmin from "../../../../../src/components/EmdashAdminClient.tsx";
+				mountEmdashAdmin();
+			</script>
 		</div>
 	</body>
 </html>
